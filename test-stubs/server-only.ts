// Empty stub used only by Vitest. The real `server-only` package throws when
// imported, but its `react-server` export is empty — and that's the behaviour
// we want for a test runner that doesn't set the `react-server` condition.
export {};
